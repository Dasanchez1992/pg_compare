# Dependencias de la app de escritorio (Electron + backend Django embebido).
#   pip install -r requirements-desktop.txt
-r requirements.txt

# Servidor WSGI de producción, puro Python y multiplataforma.
waitress>=3.0
# Sirve los archivos estáticos sin depender de DEBUG ni de un servidor web.
whitenoise>=6.6
# Empaqueta el backend en un binario autónomo (solo para construir releases).
pyinstaller>=6.6
